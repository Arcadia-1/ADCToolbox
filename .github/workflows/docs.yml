# The manual is published at adctoolbox.tokenzhang.com/doc/ by the site's own workflow, which runs exactly the sphinx
# build below against this source. Here the build is a check, and arcadia-1.github.io/ADCToolbox, where the manual used
# to live, only forwards every old address to the same page there.
name: Build and Deploy Documentation

on:
  push:
    branches:
      - main
    paths:
      - 'python/docs/**'
      - 'python/src/adctoolbox/**/*.py'  # Docstrings in source code
      - 'README.md'
      - '.github/workflows/docs.yml'
      - '.github/pages-redirects.py'
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          cd python
          pip install --upgrade pip
          pip install -e .[docs]

      - name: Build English docs
        run: |
          cd python/docs
          python -m sphinx -b html source build/html
          touch build/html/.nojekyll

      - name: Build Chinese docs
        run: |
          cd python/docs
          python -m sphinx -b html -D language=zh_CN source build/html/zh_CN

      - name: Forward the old address to the manual's new home
        run: python .github/pages-redirects.py python/docs/build/html python/docs/build/redirects

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: python/docs/build/redirects

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
